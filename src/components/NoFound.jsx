import React from 'react';

const NoFound = ({ text }) => {
    return (
        <div>
            <span className="text-gray-400 italic">{text}</span>
        </div>
    );
};

export default NoFound;